# Copilot Studio Agent SDK (Phase A - multi-turn proxy)
microsoft-agents-copilotstudio-client>=0.9.0
msal>=1.36.0

# Azure AI Foundry (alternative test case writer path)
# azure-ai-agents = the dedicated Foundry agents SDK (split from azure-ai-projects in 2.0)
azure-ai-agents>=1.1.0
azure-ai-projects>=2.0.0
azure-identity>=1.25.0

# Azure OpenAI (Phase B - parser/validator/bug analysis)
openai>=1.55.0

# FastAPI Server
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
sse-starlette>=2.0.0

# Document Processing
python-docx>=1.0.0
PyPDF2>=3.0.0

# Playwright (web consoles: Confluent + Kibana)
playwright>=1.40.0

# Data Models
pydantic>=2.6.0
pydantic-settings>=2.2.0

# JSONPath assertions
jsonpath-ng>=1.6.0

# Logging
structlog>=24.1.0

# HTTP & Utils
python-dotenv>=1.0.0
httpx>=0.27.0
aiohttp>=3.9.0

# Azure DevOps
azure-devops==7.1.0b4

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
