# ============================================================
# CORE — required for the main flow (Custom Canvas + Phase B)
# Works on Python 3.9+
# ============================================================

# FastAPI Server
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
sse-starlette>=2.0.0

# Azure OpenAI (Phase B Smart Compiler + Bug Agent)
openai>=1.55.0

# Document Processing (Word/PDF extraction)
python-docx>=1.0.0
PyPDF2>=3.0.0

# Playwright (web consoles: Confluent + Kibana)
playwright>=1.40.0

# Data Models
pydantic>=2.6.0
pydantic-settings>=2.2.0

# JSONPath assertions (Phase B Validator)
jsonpath-ng>=1.6.0

# Logging
structlog>=24.1.0

# HTTP & Utils
python-dotenv>=1.0.0
httpx>=0.27.0
aiohttp>=3.9.0
truststore>=0.9.0  # Use OS cert store (handles corporate SSL inspection — Maccabi, etc.)

# Azure DevOps (bug creation)
azure-devops==7.1.0b4

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ============================================================
# OPTIONAL — only needed for alternative paths (not the main flow)
# If install fails on older Python — comment these out, the app
# will still work with Custom Canvas + Foundry chat completions.
# ============================================================

# Microsoft 365 Agents SDK — only needed for CopilotBridgeReal (SDK path)
# Currently bypassed by Custom Canvas — safe to skip if install fails
microsoft-agents-copilotstudio-client>=0.9.0
msal>=1.36.0

# Azure AI Foundry agents SDK — only needed for Foundry agent (asst_xxx) mode
# We use Foundry chat completions directly via openai SDK, so this is NOT critical
azure-ai-agents>=1.1.0
azure-ai-projects>=2.0.0
azure-identity>=1.25.0

# ============================================================
# .NET department — Kafka + Couchbase direct clients
# OPTIONAL: required only when running #/integration/dotnet flows.
# If JFrog blocks the install — the runner returns BLOCKED with a clear
# message rather than crashing the pipeline.
# ============================================================
confluent-kafka>=2.4.0
couchbase>=4.2.0
